const tugasSembilan = () => {
  const data = ["papua", "maluku"];
  console.log(data);
  let z;
  for (z in data) {
    console.log(z);
    // ini akan menghasilkan index dari data array yang kita punya
  }
};
tugasSembilan();
